import styled from '@emotion/styled'
import {CardDiv} from './Card/Card.style'
export const LogListContainer = styled(CardDiv)({
  backgroundColor: '#FBFFB9',
  
})

export const EmptyDiv = styled.div(() => ({
  textAlign: 'center',
  fontSize: '18px',
  color: '#ccc'
}))